# ── 绝不入库 ────────────────────────────────────────────────
# 商业教材（绿宝书系列 PDF / 配套音频，约 5 GB）。版权归出版方所有，
# 仅供个人使用，不得再分发。App 的词库来自开源词表，与这里的内容无关。
data/

# 从真题 PDF 解析出来的题库。内容仍属原出版方 —— 推到公开仓库
# 比打进 App 更严重（App 只有自己装，仓库是全网可见）。
# 这也是 App 出厂不带任何试卷的同一个理由。
Docs/exams/

# ── 构建产物 ────────────────────────────────────────────────
.build/
DerivedData/
*.xcarchive
*.ipa
*.dSYM

# JMdict 编译出的 SQLite 索引（41 MB）。是生成物不是源码，重建方法见 README。
App/Resources/jmdict.sqlite

# ── Xcode ─────────────────────────────────────────────────
*.xcuserdatad/
xcuserdata/

# ── 设计稿归档 ──────────────────────────────────────────────
design/*.zip
design/logo/*.zip

# ── 系统 ──────────────────────────────────────────────────
.DS_Store
